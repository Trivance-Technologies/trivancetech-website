import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'wealthy-power-26376c166d.media.strapiapp.com'],
  },
};

export default nextConfig;
